# TributeStream Frontend Configuration
# Copy this file to .env for local development

# Environment (development, staging, production)
# PUBLIC_ENVIRONMENT=development

# API Configuration
# Base URL for API endpoints
# PUBLIC_API_BASE_URL=/api

# Strapi Backend URL (without port)
# PUBLIC_STRAPI_URL=http://localhost

# Strapi Backend Port
# PUBLIC_STRAPI_PORT=1338

# API request timeout in milliseconds
# PUBLIC_API_TIMEOUT=10000

# Retry interval for backend checks in milliseconds
# PUBLIC_RETRY_INTERVAL=10000

# Maximum number of retry attempts
# PUBLIC_MAX_RETRIES=3

# Authentication Configuration
# Name of the cookie storing the JWT
# PUBLIC_AUTH_TOKEN_NAME=jwt

# Token expiry time in seconds (default: 86400 = 1 day)
# PUBLIC_AUTH_TOKEN_EXPIRY=86400

# Feature Flags
# Enable backend availability checking
# PUBLIC_ENABLE_BACKEND_CHECK=true